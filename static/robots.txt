# Monierate robots.txt

User-agent: *
Allow: /
# Public Markets pages — pair hubs, pair x provider long-tail, provider profiles.
# Stated explicitly so the Disallow rules below are never read as covering them.
Allow: /markets/
# Exchange directory — changer profiles. The directory index and the curated
# collection pages are disabled (COLLECTIONS_ENABLED) and 404, so this rule
# effectively covers the profiles only.
Allow: /exchanges/
# Currency converter — pair pages, ladder amounts, and the per-exchange converters.
# Off-ladder amounts hold themselves back with a noindex tag, which needs the page
# to be crawlable to be read at all; a Disallow here would hide the tag instead.
Allow: /converter/
# Interactive rate-comparison pages (thin / not for organic discovery)
Disallow: /*/compare
Disallow: /buy/
Disallow: /sell/
Disallow: /send/
Disallow: /card/
# Price-alert pages
Disallow: /alerts
Disallow: /alerts-old
# API/data endpoints (not the /api landing page)
Disallow: /api/
# Parameterised URLs — canonical pages have clean paths
Disallow: /*?

Sitemap: https://monierate.com/sitemap.xml

# Blocked crawlers
User-agent: facebookexternalhit
Disallow: /

User-agent: meta-externalagent
Disallow: /

User-agent: meta-externalfetcher
Disallow: /

User-agent: Amazonbot
Disallow: /

User-agent: AlexaBot
Disallow: /

User-agent: Amazon-AdBot
Disallow: /

User-agent: Amzn-SearchBot
Disallow: /
